---
en:
  activemodel:
    errors:
      models:
        user_session:
          attributes:
            base:
              invalid_credentials: invalid login details
  helpers:
    submit:
      user_session:
        create: Log In
  layouts:
    nav_bar:
      log_out: Log Out
  simple_form:
    labels:
      user_session:
        email: Email
        password: Password
    required:
      mark: ''
