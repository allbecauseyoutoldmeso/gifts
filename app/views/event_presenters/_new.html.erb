<%= content_tag(
  :div,
  data: { show_hide_target: 'toggleable' },
  class: "show-hide-section #{'hidden' if hidden}",
  id: 'new-event'
) do %>
  <%= render(
    partial: 'event_presenters/form',
    locals: { event_presenter: new_event_presenter }
  ) %>
<% end %>
