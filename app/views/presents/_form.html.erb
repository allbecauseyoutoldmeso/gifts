<%= simple_form_for(
  [event, present],
  html: {
    data: {
      action: 'submit->requests#submitForm'
    }
  }
) do |form| %>
  <%= form.input(
    :name,
    label: false,
    input_html: { data: { focus_target: 'focusable' } }
  ) %>
  <%= form.submit %>
<% end %>
