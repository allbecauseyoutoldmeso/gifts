<%= simple_form_for(
  event_search,
  html: {
    data: {
      action: 'submit->requests#submitForm'
    }
  }
) do |form| %>
  <%= form.input(:name) %>
  <%= form.input(:recipient_id, collection: current_user.recipients) %>
  <%= form.input(:start_date, as: :date, html5: true) %>
  <%= form.input(:end_date, as: :date, html5: true) %>
  <%= form.submit %>
<% end %>
