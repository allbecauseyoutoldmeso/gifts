<%= simple_form_for(
  event,
  html: {
    data: {
      action: 'change->requests#submitForm'
    }
  }
) do |form| %>
  <%= form.input(
    :state,
    collection: state_options,
    label: false,
    include_blank: false,
    input_html: { class: 'state-option' }
  ) %>
<% end %>
